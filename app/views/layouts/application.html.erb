<!DOCTYPE html>
<html>
<head>
  <title>Vizibid | <%= yield(:title) %></title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%# yield :scripts %>
  <%= csrf_meta_tags %>
  <%= render 'layouts/shim' %>
</head>
<body>
	<%= render 'layouts/header' %>
	<div class = "container maincontent" <% if signed_in? %>style="margin-top: 60px;" <% end %>>
		<% flash.each do |key, value| %>
		<div class="alert alert-<%= key %>" style="margin-top: 25px; margin-bottom: -5px;">
			<button type="button" class="close alert-<%= key %> flashclose" data-dismiss="alert" style = "border: none;">×</button>
  			<div><%= value %></div>
		</div>
		<% end %>
		
		<%= yield %>
		
		<!-- this is required so the modal is in the maincontent div -->
		<%= render 'layouts/noaccess_share_modal' %>

		<%= render 'layouts/footer' %>
		
		<%= debug(params) if Rails.env.development? %>
	</div>
</body>
</html>
