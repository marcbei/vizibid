<%=@i%>. 
<% if f.name != nil %>
	<%= link_to f.name, f %>
<% else %>
	<%= link_to File.basename(f.form.url, File.extname(f.form.url)), f %>
<% end %>

<br/>
Submitted by: <%= link_to f.user.name, user_path(f.user.id) %>
<br/>


<% if !f.average_rating.nan? %>
	Average Rating: <%= f.average_rating %> <br/>
<% end %>

<% if f.jurisdiction %>
	<em><%= f.jurisdiction %></em>
<% end %>

<br/>

<% if f.description %>
	<%= truncante_description(f.description) %>
<% end %>


