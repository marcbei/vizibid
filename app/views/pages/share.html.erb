<% provide(:title, 'Share Forms') %>

<h1>Share Forms</h1>
<strong><p>Complete and submit this form to upload a new model legal document to Vizibid.</p></strong>

<%= form_for @form, :html => {:multipart => true} do |f| %>
  <div class="field">
    Description
  	<u><a href="#" rel="tooltip" data-placement="right" title="Please provide a thorough description of the document you are requesting. Be specific. This information will help other users provide a document that meets your needs.">?</a></u>
  	<%= f.text_area :description, :size => "10x10"%>
  	Jurisdiction
    <u><a href="#" rel="tooltip" data-placement="right" title="Select the jurisdiction where you want to use this document. If the document does not need to be jurisdiction-specific, select "Any".">?</a></u>
  	<div class = "jurisdropdown">
  		<%= f.select :jurisdiction, options_for_select(us_states) %>
  	</div>
      Keywords
      <u><a href="#" rel="tooltip" data-placement="right" title="List any helpful keywords (which can be words or phrases) that you would use in a search for the document you are requesting. Separate each word or phrase with a comma or semi-colon. The more thorough and specific your keywords are, the better.">?</a></u>
      <%= f.text_area :keywords, :size => "10x5"%>
      <%= f.file_field :form %>
  </div>
  <div class="actions">
    <%= f.submit "Submit", :class => "btn btn-large btn-success"%>
  </div>
<% end %>

